<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user.management.dao.DeptDao">
	<select id="getDeptById" resultType="Dept">
		select did,dname,dtel from udept where did=#{did}
	</select>
</mapper>